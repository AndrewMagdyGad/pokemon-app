const student = {
    name: "Ahmed",
    age: 20,
    score: 88,
};

const arr = [1, 2, 3];

const { age, score } = student;
const [name] = arr;

console.log(age);
console.log(name);
console.log(score);
